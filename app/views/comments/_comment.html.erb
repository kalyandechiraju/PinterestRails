<%= div_for(comment) do %>
	<p>
		<% unless comment.user.nil? %>
			<%= comment.user.name %> says:
		<% end %>
		<strong><%= h(comment.body) %></strong></br>
		Posted <%= time_ago_in_words(comment.created_at) %> ago
	</p>
<% end %>